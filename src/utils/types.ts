export interface Event {
    id: number;
    title: string;
    place: string;
    date: string;
    image: string;
}

/* eslint-disable no-unused-vars */
export interface ImageProps {
    id: number;
    height: string;
    width: string;
    public_id: string;
    format: string;
    blurDataUrl?: string;
  }
  